# Project notes

This is a Python project for evaluating LangGraph-style agents.

- Use Python 3.11+.
- Use `python -m venv .venv && source .venv/bin/activate` locally.
- Install with `pip install -e '.[dev]'`.
- Run tests with `pytest`.
- Run example evals with `langgraph-eval examples/scenarios`.
